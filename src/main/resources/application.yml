# 서버 포트 설정
server:
  port: 8080

  #database 연동 설정
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/super_board2?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 4865
  thymeleaf:
    cache: false
logging:
  config: classpath:logback-spring.xml

    #spring data jpa 설정
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: update

jwt:
  secret-key-source: super-board